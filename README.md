# Minecraft Seed Map JP v4.0

## v4.0 Chunkbase風Seed Map表示の強化

v4.0では、ChunkbaseのSeed Mapに近い表示体験を目標に、地形・構造物・グリッドを重ねて見るマップ表示を強化しました。

- Java版 / 統合版の両方で構造物候補を表示
- 統合版でも、村、廃ポータル、海底神殿、森の洋館、ピリジャー前哨基地、古代都市、トライアルチャンバー、要塞候補を表示
- 構造物ごとに色と短い記号を分け、マップ上で見つけやすくしました
- ホバー時に構造物名、カテゴリ、座標、エディション、候補/手動区分を確認できます
- 選択チャンク詳細に、近くの構造物候補を表示します
- 疑似バイオーム表示を強化し、草原、森林、砂漠、雪原、海、山岳、湿地、サバンナ、ジャングル風、荒野風をSeedに応じて自然に分布させます
- 表示範囲に96チャンク、128チャンクを追加し、広域表示時はチャンクの表示密度を自動調整します
- スライムチャンクON/OFF、地形ON/OFFに関係なく、構造物候補レイヤーは独立して表示されます
- 詳細バイオームは引き続き準備中です。将来cubiomes WASMに差し替え予定です
- cubiomes本体やWASMはまだ同梱していません
- 外部通信なしで動作します

構造物と統合版判定は現時点では候補表示です。実際の生成位置、バイオーム成立判定、生成失敗条件との完全一致は保証していません。

## v3.1 構造物候補のマップ反映改善と詳細バイオームPoC準備

v3.1では、構造物候補レイヤーの表示状態を確認しやすくし、将来のcubiomes WASM詳細バイオーム対応に向けたPoC入口を整理しました。

- 構造物候補の検出件数、表示中の自動候補件数、手動マーカー件数をマップ上に表示
- 表示範囲内に候補がない場合は「表示範囲内に候補がありません」と表示
- レイヤーON/OFF、カテゴリフィルタ、エディション切替、バージョン切替後に構造物候補を再描画
- 自動候補と手動マーカーの凡例表記を整理
- 詳細バイオームPoC用に `js/biome/cubiomes-provider.js` の `load()` / `isAvailable()` / `getBiomeAt(seed, version, x, y, z)` / `generateArea(seed, version, centerX, centerZ, radius)` を整備
- cubiomes本体やWASMはまだ同梱していません
- 詳細バイオームはWASM未配置のため準備中です
- 外部通信なしで動作します

構造物候補は候補表示です。実際の生成位置、バイオーム成立判定、生成失敗条件とは異なる場合があります。

## v3.0 Seed Map UI・統合版候補表示・レイヤー整理

v3.0では、Minecraft Seed Map JPをChunkbase風の「地図が主役」の画面に近づけ、操作パネル、レイヤー、構造物、地形、対応状況を整理しました。画面タイトルは「Minecraft Seed Map JP」に統一し、スライムチャンク表示は複数あるレイヤー機能の1つとして扱います。

- 左側の操作パネルに、シード値、エディション、バージョン、中心座標、表示範囲を集約
- スライムチャンク、構造物候補、手動マーカー、簡易地形を個別レイヤーとしてON/OFF可能
- 詳細バイオームは「準備中」としてUI上に明示
- Java版と統合版のバージョン選択を整理
- Java版 1.21 / 1.20 / 1.19 / 1.18 は現時点で同一候補ロジック
- 統合版 最新 / 1.21系 / 1.20系 は現時点で同一候補ロジック
- 統合版でも構造物候補レイヤーを表示
- 統合版は「候補表示対応」として扱い、結果は今後検証予定
- cubiomes WASMはまだ未同梱で、詳細バイオーム本実装は今後対応予定
- 外部通信なし、localStorage保存のみ、GitHub Pages対応

### v3.0 Java版対応状況

| 機能 | 状況 |
| --- | --- |
| スライムチャンク | 対応済み |
| 廃ポータル候補 | 候補表示 |
| 村候補 | 候補表示 |
| 海底神殿候補 | 候補表示 |
| 要塞候補 | 候補表示 |
| 森の洋館候補 | 候補表示 |
| ピリジャー前哨基地候補 | 候補表示 |
| 古代都市候補 | 候補表示 |
| トライアルチャンバー候補 | 候補表示 |
| 簡易地形 | 対応済み |
| 詳細バイオーム | 準備中 |

### v3.0 統合版対応状況

| 機能 | 状況 |
| --- | --- |
| スライムチャンク | 候補表示 |
| 構造物候補 | 候補表示 |
| バージョン選択 | 最新 / 1.21系 / 1.20系 |
| 簡易地形 | 対応済み |
| 詳細バイオーム | 準備中 |

候補表示は、実際の生成位置と異なる場合があります。完全一致、バイオーム成立判定、生成失敗条件の判定はまだ行っていません。

## v2.9 Java版要塞候補の追加

v2.9では、Java版のSeed値から要塞候補座標を算出し、構造物レイヤー上に `source: "auto"` の自動候補として表示します。要塞は村や廃ポータルなどのregion構造物とは異なるため、専用のリング候補ロジックとして分離しています。

- Java版の要塞候補を自動表示
- 表示範囲内の要塞候補のみ描画
- 構造物カテゴリフィルタの「要塞」でON/OFF可能
- 既存の構造物候補、スライムチャンク、簡易地形、地点メモ、座標変換と共存
- 要塞は候補表示のため、実際の生成位置と異なる場合があります
- 完全一致やバイオーム成立判定はまだ行っていません

## v2.8 Java版構造物候補の追加

v2.8では、Java版の構造物候補として森の洋館、ピリジャー前哨基地、古代都市、トライアルチャンバーを追加しました。既存の廃ポータル、村、海底神殿候補と同じ構造物レイヤー上に自動候補として表示され、カテゴリフィルタでON/OFFできます。

候補表示は、実際の生成位置と異なる場合があります。バイオーム成立判定や生成失敗条件の完全判定はまだ行っていません。

## v2.7 対応状況・開発状況パネル

v2.7では、画面上に「対応状況」パネルを追加し、現在使える機能、候補表示、準備中、未対応の機能を確認できるようにしました。

候補表示は、実際の生成位置と異なる場合があります。

| 機能 | 状況 |
| --- | --- |
| スライムチャンク | 対応済み |
| 廃ポータル候補 | 候補表示 |
| 村候補 | 候補表示 |
| 海底神殿候補 | 候補表示 |
| 要塞候補 | 候補表示 |
| 森の洋館候補 | 候補表示 |
| ピリジャー前哨基地候補 | 候補表示 |
| 古代都市候補 | 候補表示 |
| トライアルチャンバー候補 | 候補表示 |
| 簡易地形 | 対応済み |
| 詳細バイオーム | 準備中 |
| ネザー要塞 | 未対応 |
| エンドシティ | 未対応 |

## v2.6 cubiomes WASM PoC準備

v2.6では、将来cubiomes WASMを同梱してMinecraft Java版の正確なバイオーム判定へ進めるための配置先と読み込み入口を追加しました。現時点ではcubiomes本体やWASMは同梱していません。

- `assets/wasm/`: 将来の `cubiomes.js` / `cubiomes.wasm` 配置先
- `js/biome/cubiomes-provider.js`: 将来用の `load()` / `isAvailable()` / `getBiomeAt()` / `generateArea()` 入口
- `js/biome/cubiomes-worker.js`: 将来のWeb Worker実行入口
- `licenses/`: 将来のcubiomes MITライセンス表記配置先
- `THIRD_PARTY_NOTICES.md`: cubiomes利用予定とライセンス注意を記載
- 詳細バイオーム選択時は「cubiomes WASM未配置」と表示
- 現時点では簡易地形のみ利用可能
- cubiomes/WASMはまだ導入していません
- 外部通信なしで動作します

## v2.5 terrain provider構成

v2.5では、v2.4の簡易地形色分けレイヤーを残したまま、将来cubiomes WASMへ差し替えられるようにterrain provider構成へ整理しました。

- `simple-terrain-provider.js`: 現在の簡易地形表示を担当
- `biome-provider.js`: 地形providerの選択入口
- `cubiomes-provider.js`: 将来の詳細バイオーム用の準備中provider
- UIに地形モードを追加
- 現時点では「簡易地形」のみ利用可能
- 「詳細バイオーム：準備中」を選ぶと、描画せず準備中メッセージを表示
- cubiomes/WASMはまだ導入していません
- 外部通信なしで動作します

## v2.4 地形色分けレイヤー

v2.4では、Seed値とチャンク座標から決まる簡易的な地形・バイオーム風の色分けレイヤーを追加しました。完全なMinecraftバイオーム再現ではなく、Seed Mapとして地形の雰囲気をつかむための簡易表示です。

- スライムチャンク、構造物レイヤーとは別の地形色分けレイヤーを追加
- 地形色分けレイヤーのON/OFF切り替えに対応
- 草原、森林、砂漠、雪原、海、山岳、湿地の7種類を簡易表示
- 各チャンクの背景色を地形タイプに応じて変更
- スライムチャンクや構造物候補が見えなくならないよう、地形色は背景寄りに表示
- 地形凡例を追加
- 実際のMinecraftバイオームとは異なる場合があります
- 完全なバイオーム判定やcubiomes/WASM導入はまだ行っていません

## v2.3 海底神殿候補の自動表示

v2.3では、Java版のSeed値から表示範囲内の海底神殿候補座標を算出し、構造物レイヤー上に `source: "auto"` の自動候補として表示します。

- Java版の海底神殿候補を自動表示
- 表示中のマップ範囲内にある候補のみ描画
- 自動候補は手動登録マーカーと見た目を区別
- 構造物カテゴリフィルタの「海底神殿」でON/OFF可能
- v2.1の廃ポータル候補、v2.2の村候補表示と共存
- 海底神殿は候補表示のため、実際の生成位置と異なる場合があります
- 海洋バイオーム判定による完全な成立判定はまだ未対応です
- 外部通信なしでブラウザ内だけで計算します

## v2.2 村候補の自動表示

v2.2では、Java版のSeed値から表示範囲内の村候補座標を算出し、構造物レイヤー上に `source: "auto"` の自動候補として表示します。

- Java版の村候補を自動表示
- 表示中のマップ範囲内にある候補のみ描画
- 自動候補は手動登録マーカーと見た目を区別
- 構造物カテゴリフィルタの「村」でON/OFF可能
- v2.1の廃ポータル候補表示と共存
- 村は候補表示のため、実際の生成位置と異なる場合があります
- バイオーム判定による完全な成立判定はまだ未対応です
- 外部通信なしでブラウザ内だけで計算します

## v2.1 廃ポータル候補の自動表示

v2.1では、Java版のSeed値から表示範囲内の廃ポータル候補座標を算出し、構造物レイヤー上に `source: "auto"` の自動候補として表示します。

- Java版の廃ポータル候補を自動表示
- 表示中のマップ範囲内にある候補のみ描画
- 自動候補は手動登録マーカーと見た目を区別
- 構造物カテゴリフィルタの「廃ポータル」でON/OFF可能
- 候補表示のため、実際の生成位置と異なる場合があります
- バイオーム判定や生成失敗条件による完全一致はまだ未対応です
- 外部通信なしでブラウザ内だけで計算します

## v2.0.1 UI表記調整

v2.0.1では、画面上の表記を「スライムチャンク専用ツール」ではなく、今後の構造物表示にも対応していくSeed Mapとして分かるように調整しました。スライムチャンク表示はマップ機能の1つとして扱い、手動登録した地点・構造物マーカーは構造物レイヤー上に表示されます。

- 画面タイトルをv2.0.1へ更新
- マップ見出しを「チャンク・構造物マップ」寄りに変更
- 構造物レイヤーのON/OFF状態を画面上に表示
- v2.0.1時点ではSeedからの構造物自動検出が未対応であることを明記
- v2.0は構造物自動検出前の土台実装です

## v2.0 構造物表示レイヤー

v2.0では、今後の廃ポータル・村・要塞などの自動検出に備えて、スライムチャンクとは別の構造物表示レイヤーを追加しました。既存の地点メモ・マーカーは、手動登録された構造物として `id` / `name` / `type` / `x` / `z` / `dimension` / `source` / `note` を持つ共通データ形式で扱います。

- 手動登録構造物: `source: "manual"`
- 将来の自動検出構造物: `source: "auto"`
- 構造物レイヤーの表示ON/OFFに対応
- 既存のカテゴリ別フィルタを構造物レイヤーにも適用
- v2.0では自動検出ロジックは未実装です
- 自動検出はv2.1以降で追加予定です
- 外部通信なしで動作します
- 保存はブラウザの `localStorage` のみを使用します

追加した構造物用ファイル:

- `js/structures/config.js`
- `js/structures/layer.js`
- `js/structures/detector.js`
- `js/structures/rng.js`

Minecraftのシード値から、スライムチャンク、構造物候補、疑似バイオーム色分けを日本語UIで確認する静的Webツールです。v4.0はJava版スライムチャンク判定、統合版の候補表示対応、探索支援、地点メモ・マーカー、マップ操作、構造物管理機能、Java版と統合版の主要構造物候補表示、レイヤーON/OFF管理、Chunkbase風の疑似バイオーム表示、cubiomes WASM導入準備、対応状況パネルに対応しています。外部API、外部ライブラリ、アクセス解析は使用していません。

## 主な機能

- 数値または文字列のシード値入力
- Java版のスライムチャンク判定（対応済み）
- 統合版のスライムチャンク候補表示（今後検証が必要）
- Minecraftバージョン選択
- 中心X/Z座標と表示範囲の指定
- チャンク単位のスライムチャンクグリッド表示
- 選択チャンクのチャンク座標、ブロック範囲、中心ブロック座標、判定表示
- チャンク座標、中心ブロック座標、ブロック範囲の個別コピー
- オーバーワールド座標とネザー座標の相互変換
- 地点メモの登録、一覧表示、削除、全削除
- 表示中マップ範囲内の地点メモをマーカー表示
- 村、要塞、廃ポータルなどの構造物カテゴリ管理
- Java版の村候補自動表示
- Java版の海底神殿候補自動表示
- Java版の要塞候補自動表示
- Java版の廃ポータル候補自動表示
- Java版の森の洋館候補自動表示
- Java版のピリジャー前哨基地候補自動表示
- Java版の古代都市候補自動表示
- Java版のトライアルチャンバー候補自動表示
- 簡易地形色分けレイヤー
- 地形モード選択（簡易地形 / 詳細バイオーム準備中）
- cubiomes WASM導入用の配置先とprovider入口
- 対応状況・開発状況パネル
- カテゴリ別のマーカー色分け
- チェックボックスによるカテゴリフィルタ
- 地点名検索とカテゴリ検索
- 指定座標へのジャンプ、原点へ戻る、地点一覧からの移動
- 現在の中心チャンク座標と中心ブロック座標の表示
- スマートフォン幅に対応したレスポンシブ表示

## セットアップ

追加インストールは不要です。リポジトリを取得し、`index.html` をブラウザで開くだけで動作します。

```text
minecraft-seed-map-jp/
├── index.html
├── README.md
├── css/
│   └── style.css
├── js/
│   ├── main.js
│   ├── slime.js
│   ├── storage.js
│   └── utils.js
└── assets/
    └── icons/
        └── favicon.svg
```

## ローカル実行

このプロジェクトはビルド不要です。基本的には `index.html` をブラウザで開くだけで確認できます。

ブラウザのセキュリティ制限でJavaScriptモジュールが読み込めない場合は、プロジェクトのルートディレクトリで静的サーバーを起動してください。

```powershell
python -m http.server 8000
```

その後、ブラウザで `http://localhost:8000/` を開きます。

## 動作確認ポイント

ローカルで開いたら、以下を確認してください。

1. `index.html` を開くと、条件入力フォーム、チャンクグリッド表示エリア、地点メモ欄が表示される。
2. シード値、中心X/Z、表示範囲を入力して `マップを生成` を押すと、チャンクグリッドが生成される。
3. グリッド上のチャンクをクリックすると、チャンク座標、ブロック範囲、中心ブロック座標、スライム判定が表示される。
4. `チャンク座標をコピー`、`中心座標をコピー`、`ブロック範囲をコピー` で、必要な座標情報を個別にコピーできる。
5. ネザー座標変換で、オーバーワールド座標とネザー座標を整数のX/Zで相互変換できる。
6. 地点名、X/Z座標、カテゴリ、メモを登録すると一覧に表示される。
7. 登録地点が現在表示中のマップ範囲内にある場合、チャンク上にマーカーが表示される。
8. カテゴリごとにマーカーや一覧の色が変わる。
9. チェックボックスで全表示またはカテゴリ別表示を切り替えられる。
10. 地点名検索とカテゴリ検索で一覧とマーカーを絞り込める。
11. マーカー付きチャンクをクリックすると、チャンク詳細内に地点メモの詳細が表示される。
12. 中心X/Zを入力して `この座標へ移動` を押すと、指定座標周辺のマップに再描画される。
13. `原点へ戻る` を押すと、X=0 / Z=0 を中心に再描画される。
14. 地点一覧の `移動` を押すと、その地点周辺のマップに移動できる。
15. マップ上に現在の中心チャンク座標と中心ブロック座標が表示される。
16. ページを再読み込みしても、地点メモがブラウザの `localStorage` に残っている。
17. 画面幅を狭くしても、フォームやグリッド周辺の表示が大きく崩れない。

## GitHub Pagesでの公開

1. GitHubに `minecraft-seed-map-jp` リポジトリを作成します。
2. このディレクトリの内容を `main` ブランチへ push します。
3. GitHubの `Settings` → `Pages` を開きます。
4. `Build and deployment` を `Deploy from a branch` にします。
5. Branchを `main`、Folderを `/root` にして保存します。

## 使い方

1. シード値を入力します。
2. エディション、バージョン、中心座標、表示範囲を選びます。
3. `マップを生成` を押します。
4. グリッド上のチャンクを選択すると、チャンク座標とブロック範囲が表示されます。
5. 必要に応じて座標コピー、ネザー座標変換、地点メモを使います。
6. チャンク詳細の `この座標を地点登録に使う` で、選択チャンクの中心座標を地点登録フォームへ入れられます。
7. 中心X/Zを変更して `この座標へ移動`、または地点一覧の `移動` で表示範囲を切り替えます。

## 入力項目

- シード値: 数値または文字列。文字列はJavaの `String.hashCode` 相当で数値化します。
- 中心X/Z: ブロック座標の整数値。
- 表示範囲: 16、32、64チャンクから選択します。

## 出力内容

- チャンクグリッド: スライムチャンクは緑、通常チャンクはグレー、中心付近は枠で強調します。
- 選択チャンク詳細: チャンク座標、ブロック座標範囲、中心ブロック座標、スライム判定。
- ネザー座標変換: オーバーワールド座標とネザー座標のX/Z変換結果。小数が出る場合は四捨五入して表示します。
- 地点メモ: 地点名、X/Z座標、カテゴリ、メモ本文。
- マーカー: 現在表示中のマップ範囲内にある地点メモをチャンク上に表示します。
- 構造物カテゴリ: 村、要塞、廃ポータル、海底神殿、森の洋館、ピリジャー前哨基地、古代都市、エンドポータル、ネザー要塞、砦の遺跡、エンドシティ、トライアルチャンバー、スポナー、その他。
- フィルタと検索: カテゴリチェックボックス、地点名検索、カテゴリ検索で地点メモとマーカーを絞り込めます。
- 表示中心: 現在の中心チャンク座標と中心ブロック座標を表示します。
- 地形色分け: Seed値と座標から草原、森林、砂漠、雪原、海、山岳、湿地を簡易表示します。
- terrain provider: 現在は簡易地形のみ対応し、詳細バイオームはcubiomes/WASM対応予定です。
- cubiomes WASM: `assets/wasm/` に配置予定です。現時点では未同梱です。

## セキュリティとプライバシー

このツールは外部通信を行いません。入力されたシード値、座標、地点名、カテゴリ、メモ本文は外部サーバーへ送信されません。

地点メモはブラウザの `localStorage` にのみ保存されます。共有PCや公開端末で使う場合は、使用後に画面内の `全削除` を押すか、ブラウザのサイトデータを削除してください。

## 注意事項

- このツールは非公式のMinecraft補助ツールです。
- MinecraftおよびMojang Studiosとは関係ありません。
- Java版のスライムチャンク判定は対応済みです。
- 統合版は候補表示対応です。判定結果は今後検証が必要です。
- 構造物自動検出はJava版と統合版の候補表示に対応しています。Java版は廃ポータル、村、海底神殿、要塞、森の洋館、ピリジャー前哨基地、古代都市、トライアルチャンバー候補を表示します。統合版は候補表示として一部構造物を表示します。
- 地形色分けは簡易表示です。実際のMinecraftバイオームとは異なる場合があります。
- 詳細バイオームは準備中です。cubiomes/WASMはまだ同梱していません。
- cubiomesを将来同梱する場合はMITライセンス表記が必要です。
- 完全なバイオーム判定、3D表示、ログイン、外部API連携、広告、アクセス解析、PWAには対応していません。

## 今後の拡張候補

- メモのインポート / エクスポート
- メモ種別の色分け
- バイオームや構造物表示
- 共有URL生成
